<!-- HOST-ONLY -->
本檔＝訪談片「包裝先行」候選 SoT，由 `src/interview_autopilot.py plan` 渲染後直接餵給
`src/longform_maker/plan_gate.py`（P1 框架／P2 機器／P3 ≥8 組配對／P4 合規／P5 cluster）。

要改標題公式**改這裡，不要改程式**。三個限制：
1. 「## 包裝候選」底下每行都要是 `N. 標題 | 縮圖` 的格式，**至少 8 組**（plan_gate P3 這樣數）。
2. 「## 演算法定位」必須是獨立的 `##` 標題（不然 P3 會把它的行一起數進去）。
3. ((合規行)) 由程式填，**只有人工簽章（`--compliance-ok`）才會寫「合規：通過」**——
   不要在本檔硬寫「合規：通過」，那等於幫機器把自己的合規閘門關掉。

可用欄位：((一句話成就)) ((起點狀態)) ((核心數據)) ((示範項目)) ((cluster關鍵字)) ((合規行))
<!-- /HOST-ONLY -->
## 包裝候選（包不出來就砍題；8 組是門檻不是目標）
1. 他((一句話成就))（起點：((起點狀態))） | 來賓後台截圖「((核心數據))」紅圈放大＋左下白字「((起點狀態))」
2. ((起點狀態))的人，怎麼((一句話成就))？ | 左右分割：左「((起點狀態))」右「((核心數據))」，中間箭頭
3. 我把「((一句話成就))」的完整流程問出來了 | demo 螢幕截圖＋步驟編號 1/2/3/4 疊字
4. ((核心數據)) ← 這是怎麼辦到的 | 純數據截圖滿版＋右下大問號
5. 這個方法讓他((一句話成就)) | ((示範項目)) 操作畫面 zoom＋黃色手繪圈
6. 不是天賦：((起點狀態))也能做到的做法 | 來賓數據截圖＋大字「不是天賦」
7. 如果重來一次，他不會再做這一步 | 波形／品牌卡＋大字「別再做這步」（踩雷段）
8. 外行人((一句話成就))的完整拆解 | 來賓作品畫面＋左上小標「全程拆解」

## 演算法定位（plan_gate 讀這段）
- 框架：成就（來賓成就框架；訪談＝把「成就框架」的供給量從自己一個人放大到整個來賓池）
- 機器：推薦（訪談沒有搜尋量，靠推薦機的 CTR/AVD；來賓自帶外部點火）
- cluster關鍵字：((cluster關鍵字))（訪談＝同一條線換格式，不是換線）
((合規行))
- Expected Views 對標：與同窗長片同 SOP（D2/D7/D28）；系列判生死看 3 集平均，不看單集
